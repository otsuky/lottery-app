// import React from 'react'

// const SortedList = ({ todos, active }) => (
//   <div>
//     {todos.map(todo => (
//       <div key={todo.id} disabled={ active }>{todo.text}</div>
//     ))}
//   </div>

// )

// export default SortedList
